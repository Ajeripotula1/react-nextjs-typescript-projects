# React + Next.js + TypeScript Projects

This repository contains multiple hands-on projects built while following
the "Complete React, Next.js & TypeScript Projects Course 2025" on Udemy.

Each project is independent, uses its own dependencies, and can be run via:

```bash
cd project-name
npm install
npm run dev
```
